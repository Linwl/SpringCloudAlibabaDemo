spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: gateway-service
  cloud:
    nacos:
      config:
        server-addr: 192.168.1.107:8848
        file-extension: yaml

  profiles:
    active: dev